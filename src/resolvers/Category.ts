// import { Resolvers } from '../generated/types';

// const Category: Resolvers['Category'] = {
//     products: async (category, _args, { prisma }) => {
//         return prisma.category.findUnique({ where: { id: category.id } }).products();
//     },
// };

// export default Category;