// import { Resolvers } from '../generated/types';

// const Product: Resolvers['Product'] = {
//     categories: async (product, _args, { prisma }) => {
//         return prisma.product.findUnique({ where: { id: product.id } }).categories();
//     },
// };

// export default Product;